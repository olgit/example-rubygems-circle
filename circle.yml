# If your build needs more advanced behavior, see detailed Circle CI instructions:
# https://circleci.com/docs/1.0/language-ruby-on-rails/

test:
  post:
    - bash <(curl -s https://test.duckbuild.io/ci/circle/scripts/upload)
